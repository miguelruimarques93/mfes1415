class DateTest
	instance variables
		d: Date := new Date(30, 1, 2010)
	
	operations
		private assertTrue: bool ==> ()
    assertTrue(cond) == return
    pre cond;
    
    private assertFalse: bool ==> ()
    assertFalse(cond) == return
    pre not cond;
		
		private testLeapYear: () ==> ()
		testLeapYear() ==
		(
			assertTrue(Date`isLeapYear(2000));
			assertTrue(Date`isLeapYear(2004));
			assertFalse(Date`isLeapYear(2100));
			assertFalse(Date`isLeapYear(2001));
			
			IO`println(new Date(29, 2, 2000));
			IO`println(new Date(28, 2, 2001));
			IO`println(new Date(30, 4, 2001));
			
			
		);
		
		private testGetters : () ==> ()
		testGetters() == 
		(
			assertTrue(d.getDay() = 30);
			assertTrue(d.getMonth() = 1);
			assertTrue(d.getYear() = 2010);
		);
		
		private testComparators : () ==> ()
		testComparators() ==
		(
			let d1 : Date = new Date(1, 1, 2000), 
				  d2 : Date = new Date(1, 1, 2010) in (
				assertTrue(d1.isLessOrEqual(d2));
				assertTrue(d1.isLessOrEqual(d1));
				assertTrue(d2.isGreater(d1));
			);
		);
		
		public static main: () ==> ()
    main() ==
    (
    	let dt : DateTest = new DateTest() in (
      	dt.testLeapYear();
      	dt.testGetters();
      	dt.testComparators();
      );
    );

end DateTest
